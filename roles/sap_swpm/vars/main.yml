# SPDX-License-Identifier: Apache-2.0
---

__sap_swpm_inifile_vars_to_be_asserted:
  - name: sap_swpm_fqdn
    check: length_greater_zero
    sections:
      - nw_config_other

  - name: sap_swpm_set_fqdn
    check: boolean
    sections:
      - nw_config_other

  - name: sap_swpm_ascs_instance_hostname
    check: length_greater_zero
    sections:
      - nw_config_central_services_abap
      - db_config_anydb_sapase

  - name: sap_swpm_ascs_instance_nr
    check: two_digit_number
    sections:
      - nw_config_central_services_abap
      - nw_config_ports

  - name: sap_swpm_ers_instance_hostname
    check: length_greater_zero
    sections:
      - nw_config_ers

  - name: sap_swpm_ers_instance_nr
    check: two_digit_number
    sections:
      - nw_config_ers

  - name: sap_swpm_pas_instance_hostname
    check: length_greater_zero
    sections:
      - nw_config_primary_application_server_instance

  - name: sap_swpm_pas_instance_nr
    check: two_digit_number
    sections:
      - nw_config_primary_application_server_instance

  - name: sap_swpm_aas_instance_hostname
    check: length_greater_zero
    sections:
      - nw_config_additional_application_server_instance

  - name: sap_swpm_aas_instance_nr
    check: two_digit_number
    sections:
      - nw_config_additional_application_server_instance

  - name: sap_swpm_sapadm_uid
    check: length_greater_zero
    sections:
      - sap_os_linux_user

  - name: sap_swpm_sapsys_gid
    check: length_greater_zero
    sections:
      - sap_os_linux_user

  - name: sap_swpm_sidadm_uid
    check: length_greater_zero
    sections:
      - sap_os_linux_user

  - name: sap_swpm_master_password
    check: length_greater_zero
    sections:
      - credentials
      - credentials_anydb_sapase
      - nw_config_central_services_java

  - name: sap_swpm_ddic_000_password
    check: length_greater_zero
    sections:
      - credentials_syscopy

  - name: sap_swpm_sap_sidadm_password
    check: length_greater_zero
    sections:
      - credentials
      - credentials_anydb_ibmdb2
      - credentials_anydb_oracledb
      - credentials_anydb_sapase
      - credentials_anydb_sapmaxdb
      - maintenance_plan_stack_sum_config

  - name: sap_swpm_db_system_password
    check: length_greater_zero
    sections:
      - credentials_anydb_oracledb
      - credentials_anydb_sapase
      - credentials_anydb_sapmaxdb
      - credentials_hana
      - db_connection_nw_hana

  - name: sap_swpm_backup_system_password
    check: length_greater_zero
    sections:
      - db_restore_hana

  - name: sap_swpm_db_schema_java_password
    check: length_greater_zero
    sections:
      - db_connection_nw_hana

  - name: sap_swpm_db_host
    check: length_greater_zero
    sections:
      - db_config_anydb_ibmdb2
      - db_config_anydb_sapase
      - db_connection_nw_hana
      - db_restore_hana
      - syscopy_import_anydb_ibmdb2

  - name: sap_swpm_db_schema_abap
    check: length_greater_zero
    sections:
      - db_config_anydb_oracledb
      - db_connection_nw_anydb_oracledb
      - db_connection_nw_hana

  - name: sap_swpm_db_schema_java
    check: length_greater_zero
    sections:
      - db_connection_nw_hana

  - name: sap_swpm_backup_location
    check: length_greater_zero
    sections:
      - db_restore_hana

  - name: sap_swpm_backup_prefix
    check: length_greater_zero
    sections:
      - db_restore_hana

  - name: sap_swpm_db_sid
    check: length_greater_zero
    sections:
      - db_config_anydb_ibmdb2
      - db_config_anydb_oracledb
      - db_config_anydb_oracledb_121
      - db_config_anydb_oracledb_122
      - db_config_anydb_oracledb_19
      - db_config_anydb_sapmaxdb
      - db_connection_nw_hana
      - db_restore_hana
      - syscopy_import_anydb_ibmdb2
