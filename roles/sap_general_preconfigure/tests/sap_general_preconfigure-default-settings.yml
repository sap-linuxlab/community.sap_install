---
- name: Play for testing the role sap_general_preconfigure
  hosts: all

  tasks:
    - name: Import the role sap_general_preconfigure
      ansible.builtin.import_role:
        name: community.sap_install.sap_general_preconfigure
