---
sap_ha_install_pacemaker_sid: "{{ sap_hana_sid }}"
sap_ha_install_pacemaker_instance_number: "{{ sap_hana_instance_number }}"

sap_ha_install_pacemaker_cluster_name: "{{ sap_hana_cluster_name }}"
sap_ha_install_pacemaker_cluster_nodes: "{{ sap_hana_cluster_nodes }}"

sap_ha_install_pacemaker_vip1: " {{ sap_hana_vip1 }}}"
sap_ha_install_pacemaker_vip2: " {{ sap_hana_vip2 }}}"

sap_ha_install_pacemaker_hacluster_password: "{{ sap_hana_hacluster_password }}"

sap_ha_install_pacemaker_stonith_devices: "{{ sap_pacemaker_stonith_devices }}"
sap_ha_install_pacemaker_stonith_timeout: "{{ sap_pacemaker_stonith_timeout | default('900') }}"

sap_ha_install_pacemaker_stonith_sbd_devices: "{{ sap_pacemaker_stonith_sbd_devices }}"
sap_ha_install_pacemaker_stonith_sbd_watchdog_module: "{{ sap_pacemaker_stonith_sbd_watchdog_module | default('softdog')}}"
sap_ha_install_pacemaker_stonith_sbd_watchdog_module_blocklist: "{{ sap_pacemaker_stonith_sbd_watchdog_module_blocklist }}"
sap_ha_install_pacemaker_stonith_sbd_timeout_watchdog: "{{ sap_pacemaker_stonith_sbd_timeout_watchdog | default('30') }}"
sap_ha_install_pacemaker_stonith_sbd_timeout_msgwait: "{{ sap_pacemaker_stonith_sbd_timeout_msgwait | default('60') }}"
