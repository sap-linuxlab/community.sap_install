---
enable_list:
  - yaml
skip_list:
  - ignore-errors # We use ignore_errors for all the assert tasks, which should be acceptable
  - var-spacing # An attempt to reduce the number of errors in the ansible-lint GitHub run
